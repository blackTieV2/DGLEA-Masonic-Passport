# shared/contracts

Shared API/domain contracts and generated artifacts used by multiple components.

Examples:
- OpenAPI-derived types
- stable DTO contracts
- shared enums that are explicitly versioned

Contract changes should be versioned and coordinated with consumers.
