/**
 * Integration placeholder: auth/session
 */
describe('auth/session (integration)', () => {
  it.todo('POST /auth/login returns access and refresh tokens for valid credentials');
  it.todo('POST /auth/login rejects invalid credentials with UNAUTHENTICATED');
  it.todo('GET /auth/me returns scoped identity context for authenticated user');
  it.todo('POST /auth/refresh rotates/returns valid access token');
  it.todo('POST /auth/logout invalidates active session token');
});
