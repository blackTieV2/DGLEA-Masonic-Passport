# backend/db/seeds

Seed placeholders only.

## Rules
- Do not store fake production member data.
- Keep only environment-safe baseline reference seed scripts.
- Use idempotent SQL patterns where possible.

Current seed files in this folder are templates/placeholders.
